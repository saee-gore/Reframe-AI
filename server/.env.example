# ── Tavus credentials ──────────────────────────────────────────────────────
TAVUS_API_KEY=[Replace Me]
TAVUS_REPLICA_ID=[Replace Me]
# TAVUS_VOICE_ID=   # Optional — leave blank for replica default

# ── Server ─────────────────────────────────────────────────────────────────
PORT=3001
CLIENT_URL=http://localhost:5173

# ── Webhooks (fill in after running ngrok) ──────────────────────────────────
WEBHOOK_URL=https://[Replace Me].ngrok-free.app/api/webhook/tavus
